---
description: Intelligently organize files and folders - find duplicates, suggest structures, automate cleanup
argument-hint: Optional directory path to organize
---

# File Organizer

Load the `awesome-claude-skills:file-organizer` skill and follow its workflow to organize files.

If `$ARGUMENTS` is provided, use it as the target directory to organize.

Otherwise, ask the user which directory they want organized.
