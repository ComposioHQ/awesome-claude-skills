---
name: gsd:debug
description: Systematic debugging with persistent state across context resets. Uses a scientific method approach with a debug session log so you don't lose progress when context resets.
---

## gsd:debug
**Category:** GSD (Get Stuff Done)

**What it does:**
Systematic debugging with persistent state across context resets. Uses a scientific method approach: hypothesis → test → conclusion. Maintains a debug session log so you don't lose progress when context resets.

**When to trigger:**
When you have a bug that needs structured investigation.

**How to install GSD:**
```bash
npx claude install superpowers
```

**Trigger phrase:** `/gsd:debug` — for persistent, structured bug investigation.
